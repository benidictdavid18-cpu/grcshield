# Included at the server level and again in every location that sets its own
# add_header, because an add_header inside a location replaces all of the server's.
# The bundle is a module script with no inline scripts, so script-src is 'self'.
# Styles allow inline because the build emits a few style attributes.
add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'" always;
add_header X-Content-Type-Options nosniff always;
add_header X-Frame-Options DENY always;
add_header Referrer-Policy no-referrer always;
